<div>
  <h1 class="font-bold text-4xl">Electoral Wards</h1>
  <div class="flex flex-col gap-y-4">
    <% @electoral_wards.each do |ward| %>
      <div class="p-2 rounded bg-slate-50">
        <h2 class="text-2xl"><%= ward.name %></h2>
      </div>
    <% end %>
  </div>
</div>
